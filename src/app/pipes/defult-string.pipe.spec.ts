import { DefultStringPipe } from './defult-string.pipe';

describe('DefultStringPipe', () => {
  it('create an instance', () => {
    const pipe = new DefultStringPipe();
    expect(pipe).toBeTruthy();
  });
});
